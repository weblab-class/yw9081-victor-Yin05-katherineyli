import React, { useState, useEffect } from "react";
import { Link } from "react-router-dom";

const HomeIcon = () => {
  return(
  <div>
    <Link to="/" >
        Beast Mode Home Icon
    </Link>
  </div>
  )
};

// To be replaced with an image, most likely.

export default HomeIcon;
